# Optional. The core PackLab 3D app starts without these packages.
torch
segment-anything
# TripoSR is not published as a normal PyPI package. Install the upstream
# VAST-AI-Research/TripoSR project and make its `tsr` package importable.
